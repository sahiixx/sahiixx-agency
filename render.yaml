services:
  - type: web
    name: opa-demo
    runtime: python
    buildCommand: "pip install -e ."
    startCommand: "uvicorn sahiixx_agency.api.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHONPATH
        value: ""
    plan: free
